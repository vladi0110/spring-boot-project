server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cars
    username: root
    password: password

  jpa:
    databas: MYSQL
    show-sql: true
    hibernate:
      ddl-auto: validate